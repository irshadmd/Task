#Live :https://ugbitetask.000webhostapp.com/

-Configure database
  1. Open create database named "watch"
  2. Goto import and choose sql file which is inside the "database" folder named as "watch.sql"

